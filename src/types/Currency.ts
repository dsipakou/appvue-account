export interface Currency {
  id: number,
  code: string,
  sign: string,
  verbalName: string,
  isDefault: boolean,
  isBase: boolean,
  comments: string,
  createdAt: string,
  updatedAt: string,
}
